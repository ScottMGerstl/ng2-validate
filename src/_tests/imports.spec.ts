import { BaseValidation, IFieldValidation, IFieldValidatorResult, ValidationMessage, ValidationMessageComponent, ValidationMessages, ValidationMessagesComponent } from '../core';
import { HasValue, IsEmail, IsPattern, MaxLength, MinLength } from '../logic';